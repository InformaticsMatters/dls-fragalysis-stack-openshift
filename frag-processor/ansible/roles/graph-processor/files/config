process {
    container = 'busybox:latest'
    scratch = false
}

singularity {
    enabled = true
    cacheDir = "$HOME/singularity-containers"
    autoMounts = true
}

executor = 'ignite'
executor {
    queueSize = 100
}

cluster {
    join = "path:$HOME/cluster"
    master.maxCpus = 0
}
