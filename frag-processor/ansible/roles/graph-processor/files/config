process {
    executor = 'slurm'
    container = 'busybox:latest'
    scratch = true
}

singularity {
    enabled = true
    cacheDir = "$HOME/singularity-containers"
    autoMounts = true
}

executor {
    queueSize = 100
}
